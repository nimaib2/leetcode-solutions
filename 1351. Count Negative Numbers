class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        count=0
        for subset in grid:
            subset.sort()
            for num in subset:
                if num>=0:
                    break
                else:
                    count+=1
        
        return count
        
